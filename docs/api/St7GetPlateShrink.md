# St7GetPlateShrink

Returns a percentage by which to shrink the display of plate elements.

long St7GetPlateShrink(long uID, long* Shrink)
Input Parameters
uID
Strand7 model file ID.
Output Parameters
Shrink
The Shrink percentage in the range kMinShrink to kMaxShrink.
